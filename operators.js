/*

Operadores aritméticos

O operador de multiplicação é o * (asterisco);

O operador de divisão é a / (barra);

O operador de soma é o + (positivo);

O operador de subtração é o - (negativo).

Outros operadores aritméticos incluem o resto de divisão, incremento, decremento e exponencial, com exemplos a seguir:

/*

/* resto da divisão: sinal % */

let remainder
remainder = 11 % 9
console.log(remainder)

/* incremento: sinal ++ */
let increment = 0

console.log(++increment)
console.log(increment)

/* decremento: sinal -- */
let decrement = 0
decrement--
console.log(decrement)

/* exponencial: sinal ** */
console.log(2 ** 3)


let one = 1
let two =2

console.log (one == two) //retorna false
console.log (onte != two) // retorna true

// o terceiro igual === significa que está comparando inclusive o tipo do dados
// === estritamento igual e ==! estritamente diferente
// >- mauior ou igual, <= menor ou igual, > maior e < menor

// operadores de assignment ou atribuição

let x =1
x -= 1 // x = x - 1
x += 1 // x = x + 1
x *= 5 // x = 5 x

// AND = &&
// OR = ||
// NOT = ! Negação altera o valor dela se tiver true vira false e vice versa





